import { createPullRequestContext } from "../test/utils/mockContext.ts";

// Reuse test utility for consistency
const pullRequestContext = createPullRequestContext();

export default pullRequestContext;