import { createWorkflowDispatchContext } from "../test/utils/mockContext.ts";

// Reuse test utility for consistency
const workflowDispatchContext = createWorkflowDispatchContext();

export default workflowDispatchContext;